@page
@model ErrorModel
@{
    ViewData["Title"] = "发生错误";
}

<h1 class="h4 text-danger">处理请求时发生错误</h1>
<p>请稍后重试；如果问题持续出现，请携带下面的请求编号查看服务日志。</p>

@if (Model.ShowRequestId)
{
    <p>
        <strong>请求编号：</strong><code>@Model.RequestId</code>
    </p>
}
